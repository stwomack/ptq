---
instances: 1
memory: 4GB
applications:
  - name: mastercard-ess-query
    host: ptq
    buildpack: java_buildpack_offline
    path: target/ptq-0.0.1.jar
    hosts:
      - ptq
    services:
      - ess-postgres
