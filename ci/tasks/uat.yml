---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

inputs:
  - name: build-output

outputs:
  - name: uat-output

run:
  path: build-output/ci/tasks/uat.sh